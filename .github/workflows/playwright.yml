name: Playwright Tests

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch: # Кнопка для ручного запуска

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-node@v4
      with:
        node-version: lts/*

    # Настройка Java для работы Allure
    - name: Setup Java
      uses: actions/setup-java@v3
      with:
        distribution: 'microsoft'
        java-version: '17'

    - name: Install dependencies
      run: npm ci

    - name: Install Playwright Browsers
      run: npx playwright install --with-deps

    - name: Run Playwright tests
      run: npx playwright test
      continue-on-error: true # Позволяет собрать отчет, даже если тесты упали

    # Генерация Allure отчета
    - name: Generate Allure Report
      run: |
        npm install -g allure-commandline
        allure generate allure-results --clean -o allure-report

    # Сохранение HTML-отчета и Allure как артефактов
    - name: Upload Reports
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: all-reports
        path: |
          playwright-report/
          allure-report/
        retention-days: 30

    # Опционально: Отправка Email (настрой Secrets на GitHub, если хочешь, чтобы работало)
    # - name: Send Email
    #   uses: dawidd6/action-send-mail@v3
    #   if: always()
    #   with:
    #     server_address: smtp.gmail.com
    #     server_port: 465
    #     username: ${{ secrets.MAIL_USERNAME }}
    #     password: ${{ secrets.MAIL_PASSWORD }}
    #     subject: "Playwright Test Results"
    #     to: "your-email@example.com"
    #     body: "Tests finished. Check GitHub Actions for reports."